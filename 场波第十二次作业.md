### **P5.4.1**

**(a)**

设天线位于 $y = \pm d/2$。

由波程差引起的相位差为：$\Delta = k \frac{d}{2} \sin\phi$

总阵列因子为两天线辐射场之和：

$$F(\phi) = e^{i\Delta} + e^{i\psi} e^{-i\Delta}$$

提取公因子 $e^{i\psi/2}$ 并利用欧拉公式化简：

$$F(\phi) = e^{i\psi/2} \left[ e^{i(\Delta - \psi/2)} + e^{-i(\Delta - \psi/2)} \right] = 2e^{i\psi/2} \cos\left(\frac{kd}{2}\sin\phi - \frac{\psi}{2}\right)$$

取模即得证：

$$|F(\phi)| = \left| 2\cos\left(\frac{kd}{2}\sin\phi - \frac{\psi}{2}\right) \right|$$

**(b)**

令余弦项内的宗量 $u = \frac{kd}{2}\sin\phi - \frac{\psi}{2}$。

(i) 要求主瓣向北 ($\phi = 90^\circ$): 此时 $\sin\phi = 1$，对应 $u = 0$。

$$\frac{kd}{2} - \frac{\psi}{2} = 0 \implies \psi = kd$$

(ii) 要求零点仅在 ($\phi = 225^\circ, 315^\circ$): 此时 $\sin\phi = -\frac{\sqrt{2}}{2}$。

代入 $u$，为保证该处为唯一的零点，该宗量必须等于第一零点 $-\pi/2$：

$$-\frac{kd}{2}\left(1 + \frac{\sqrt{2}}{2}\right) = -\frac{\pi}{2} \implies kd = \pi(2-\sqrt{2})$$

**结论：**

- 间距：$d = \frac{2-\sqrt{2}}{2}\lambda \approx 0.293\lambda$
    
- 相位差：$\psi = \pi(2-\sqrt{2}) \approx 0.586\pi$
    
- 布置方式：两天线塔沿南北方向放置，南塔馈电相位超前北塔 $\psi$。
    

### **P5.4.3**

**(a)**

代入 $u=0$，并利用半角公式展开：

$$|\tilde{F}(0)| = \frac{\sin(\frac{\pi}{N-1})}{1 - \cos(\frac{\pi}{N-1})} = \frac{2\sin(\frac{\pi}{2(N-1)})\cos(\frac{\pi}{2(N-1)})}{2\sin^2(\frac{\pi}{2(N-1)})} = \cot\left(\frac{\pi}{2(N-1)}\right)$$

当 $N \to \infty$ 时，$\frac{\pi}{2(N-1)} \to 0$。由 $\cot x \sim 1/x$ 展开可得渐近值：

$$|\tilde{F}(0)| \sim \frac{2(N-1)}{\pi}$$

**(b)**

**1. 修正零点条件推导：**

阵列因子为 $|\tilde{F}(u)| = \left| \frac{\cos(\frac{N-1}{2}u)\sin(\frac{\pi}{N-1})}{\cos u - \cos\frac{\pi}{N-1}} \right|$。

零点需满足分子为 $0$ 且分母不为 $0$：

- 分子为 $0$：$\cos\left(\frac{N-1}{2}u\right) = 0 \implies u = \frac{(2m+1)\pi}{N-1}, \quad m \in \mathbb{Z}$
    
- 分母为 $0$（奇点）：$\cos u - \cos\frac{\pi}{N-1} = 0 \implies u = 2k\pi \pm \frac{\pi}{N-1}, \quad k \in \mathbb{Z}$
    

令二者相等，寻找需要剔除的 $m$ 值：

$$\frac{(2m+1)\pi}{N-1} = 2k\pi \pm \frac{\pi}{N-1} \implies 2m+1 = 2k(N-1) \pm 1$$

取 $+$ 号：$2m = 2k(N-1) \implies \frac{m}{N-1} = k \in \mathbb{Z}$

取 $-$ 号：$2m+2 = 2k(N-1) \implies \frac{m+1}{N-1} = k \in \mathbb{Z}$

结论：讲义中 $m/(N+1)$ 的条件系错误。正确的零点应满足 $u = \frac{(2m+1)\pi}{N-1}$，且 $\frac{m}{N-1} \notin \mathbb{Z}$ 并满足 $\frac{m+1}{N-1} \notin \mathbb{Z}$。

**2. 极值与旁瓣分析：**

在 $u = \frac{\pi}{N-1}$ 处，令 $u = \frac{\pi}{N-1} + \epsilon$，对其进行泰勒一阶展开：

$$|\tilde{F}(u)| \approx \left| \frac{-\frac{N-1}{2}\epsilon}{-\sin(\frac{\pi}{N-1})\epsilon} \right| = \frac{N-1}{2\sin(\frac{\pi}{N-1})} \neq 0$$

由于此处一阶导数不为 $0$，故 $u = \frac{\pi}{N-1}$ 不是局部最大值。

极值点满足 $\frac{d}{du}|\tilde{F}| = 0$，得到：

$$\frac{N-1}{2} \tan\left(\frac{N-1}{2}u\right) = \frac{\sin u}{\cos u - \cos(\frac{\pi}{N-1})}$$

令 $v = \frac{N-1}{2}u$，取 $N \to \infty$ 的渐近极限：

$$\tan v \approx \frac{8v}{\pi^2 - 4v^2}$$

解得第一旁瓣位置 $v_1 \approx 5.944$，对应的渐近旁瓣电平 $SLL \approx -23$ dB。

**(c)**

已知 $N=6, d=\lambda/2 \implies kd=\pi$。

分母积分（利用 Parseval 定理）：

$$\int_{-\pi}^{\pi} |\tilde{F}(u)|^2 du = 2\pi \sum_{n=0}^{5} \sin^2\left(\frac{n\pi}{5}\right) = 2\pi \left(\frac{5}{2}\right) = 5\pi$$

分子项为：

$$2kd \cot^2\left(\frac{\pi}{2(N-1)}\right) = 2\pi \cot^2\left(\frac{\pi}{10}\right) = 2\pi \cot^2(18^\circ)$$

代入方向性系数 $D$ 并化简：

$$D = \frac{2\pi \cot^2(18^\circ)}{5\pi} = \frac{2}{5} \left(\frac{5+\sqrt{5}}{3-\sqrt{5}}\right) = \frac{2}{5} (5 + 2\sqrt{5})$$

$$D = 2 + \frac{4\sqrt{5}}{5} \approx 3.789$$